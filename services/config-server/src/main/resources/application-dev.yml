# Dev profile: wide actuator surface to make local debugging of config-server
# easier (no datasource, no large business package to debug here).
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
