# Profil dev : api-gateway n'a pas de base de données (voir README, data model), donc
# pas de bloc spring.datasource ici.
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.edgareldy.springmicroservicestutorial.apigateway: DEBUG
