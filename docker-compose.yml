# Started in feature/infrastructure (postgres, discovery-server, config-server)
# and grown incrementally, one business service per feature branch, per the
# README's "Order of work". Still missing here: customer-service,
# order-service, notification-service, api-gateway, kafka, redis, zipkin.
#
# Note on networking: config-repo/*.yml (shared, committed configuration also
# used to run services locally straight from the IDE or `mvn spring-boot:run`)
# still hardcodes localhost for anything environment-agnostic (Eureka's
# defaultZone, still localhost:8761 for every profile including "prod": no
# per-environment override exists for it yet). Anything that already varies
# per Spring profile instead goes through env vars resolved to the `infra`
# network's Docker Compose service names below (auth-service's DB_URL,
# CONFIG_SERVER_URL, JWT_SECRET, all only wired for the "prod" profile used in
# this compose file). Eureka's still-hardcoded-localhost is a known gap,
# expected to be addressed in a later branch (api-gateway/observability) once
# every business service actually runs inside this compose file and it is
# worth introducing a profile-based override for it too.

networks:
  infra:
    name: spring-microservices-tutorial-infra
    driver: bridge

volumes:
  postgres-data:

services:
  postgres:
    image: postgres:16
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      # Read-only mount: scripts here run once, only against an empty data
      # directory, to create auth_db/catalog_db/customer_db/order_db.
      - ./docker/postgres-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - infra

  discovery-server:
    build:
      context: .
      dockerfile: services/discovery-server/Dockerfile
    container_name: discovery-server
    ports:
      - "8761:8761"
    networks:
      - infra
    # No datasource, no dependency on postgres. Nothing else needs to be
    # up before Eureka itself starts.

  config-server:
    build:
      context: .
      dockerfile: services/config-server/Dockerfile
    container_name: config-server
    ports:
      - "8888:8888"
    depends_on:
      # Ordering only, not a hard runtime requirement: config-server serves
      # config-repo/*.yml from its own classpath and never calls Eureka to
      # do so. Starting discovery-server first just avoids every business
      # service (added in later branches) racing config-server for a
      # registry that isn't there yet on first `docker compose up`.
      - discovery-server
    networks:
      - infra

  auth-service:
    build:
      context: .
      dockerfile: services/auth-service/Dockerfile
    container_name: auth-service
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      # Unlike config-repo/*.yml (still localhost-based, see the note at the
      # top of this file), CONFIG_SERVER_URL is already an env-var override
      # built into auth-service's local application.yml
      # (spring.config.import: "optional:configserver:${CONFIG_SERVER_URL:...}"),
      # so overriding it here to the Docker Compose service name does not
      # require touching any committed config-repo YAML. Without this,
      # auth-service would fail to fetch auth-service-prod.yml at all (the
      # import is optional, so it would start with no datasource/JWT config
      # instead of failing loudly), since "localhost:8888" inside this
      # container is auth-service itself, not config-server.
      CONFIG_SERVER_URL: http://config-server:8888
      # postgres/postgres are the POSTGRES_USER/POSTGRES_PASSWORD of the
      # `postgres` service above; `postgres` (not localhost) is its Docker
      # Compose service name, resolvable on the shared `infra` network.
      DB_URL: jdbc:postgresql://postgres:5432/auth_db
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      # Demo-only value so `docker compose up` works out of the box without
      # requiring a secret to be exported first; never a real production
      # secret. A real deployment must override this (see
      # config-repo/auth-service-prod.yml, ${JWT_SECRET} has no default).
      JWT_SECRET: demo-only-jwt-secret-do-not-use-in-real-production-0123456789-auth-service
    depends_on:
      postgres:
        # Hard requirement, unlike discovery-server/config-server below:
        # auth-service's DataSource bean fails fast at startup if it cannot
        # reach Postgres at all, so it must wait for the healthcheck, not
        # just for the container process to exist.
        condition: service_healthy
      discovery-server:
        condition: service_started
      config-server:
        condition: service_started
    networks:
      - infra
    # No Kafka service in this compose file yet (added in
    # feature/notification-service, per the README's "Order of work").
    # spring-kafka's KafkaTemplate/KafkaProducer connect lazily: nothing at
    # application startup talks to a broker. AuthEventProducer only touches
    # Kafka when publishUserRegistered/publishPasswordResetRequested is
    # actually called (register/forgot-password requests), so auth-service
    # is expected to start and serve every non-event-publishing endpoint
    # normally without Kafka present. Risk to be aware of: KafkaTemplate.send
    # returns a future that AuthEventProducer does not currently wait on or
    # attach a callback to, so a send against an unreachable kafka:9092 fails
    # asynchronously (visible only in producer logs, per
    # ProducerConfig max.block.ms, default 60s before the internal metadata
    # fetch times out) rather than surfacing as an HTTP error to the caller;
    # this is unchanged until feature/notification-service adds a real
    # `kafka` service here.

  catalog-service:
    build:
      context: .
      dockerfile: services/catalog-service/Dockerfile
    container_name: catalog-service
    ports:
      - "8082:8082"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      # Same rationale as auth-service above: overriding it here to the Docker
      # Compose service name does not require touching any committed
      # config-repo YAML, since catalog-service's local application.yml already
      # builds the import URL from this env var.
      CONFIG_SERVER_URL: http://config-server:8888
      # postgres/postgres are the POSTGRES_USER/POSTGRES_PASSWORD of the
      # `postgres` service above; `postgres` (not localhost) is its Docker
      # Compose service name, resolvable on the shared `infra` network.
      DB_URL: jdbc:postgresql://postgres:5432/catalog_db
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      # MUST be the exact same value as auth-service's JWT_SECRET above:
      # catalog-service only parses/verifies JWTs, it never issues one, and
      # verification only succeeds if both services sign/verify with the same
      # HMAC secret. Demo-only value, never a real production secret (see
      # config-repo/catalog-service-prod.yml, ${JWT_SECRET} has no default).
      JWT_SECRET: demo-only-jwt-secret-do-not-use-in-real-production-0123456789-auth-service
    depends_on:
      postgres:
        # Hard requirement, unlike discovery-server/config-server below:
        # catalog-service's DataSource bean fails fast at startup if it cannot
        # reach Postgres at all, so it must wait for the healthcheck, not
        # just for the container process to exist.
        condition: service_healthy
      discovery-server:
        condition: service_started
      config-server:
        condition: service_started
    networks:
      - infra
